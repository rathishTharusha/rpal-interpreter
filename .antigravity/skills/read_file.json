{
    "name": "read_file",
    "description": "Reads the exact contents of a specified file. Can return the entire file or a highly specific subset of lines if start_line and end_line parameters are intelligently defined.",
    "strict": true,
    "parameters": {
        "type": "object",
        "properties": {
            "file_path": {
                "type": "string",
                "description": "The precise absolute or relative path to the target file to be ingested."
            },
            "start_line": {
                "type": "integer",
                "description": "The 1-indexed line number where reading should commence. If reading the entire file is absolutely necessary, set this to 1.",
                "minimum": 1
            },
            "end_line": {
                "type": "integer",
                "description": "The 1-indexed line number where reading should terminate. To read from the start_line until the exact end of the file, set this to -1."
            }
        },
        "required": [
            "file_path",
            "start_line",
            "end_line"
        ],
        "additionalProperties": false
    }
}